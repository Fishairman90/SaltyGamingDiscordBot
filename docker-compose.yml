services:
  saltygamingdiscordbot:
    build: .
    container_name: saltygamingdiscordbot
    env_file:
      - .env
    networks:
      - docker_network
    restart: unless-stopped

networks:
  docker_network:
    external: true